name: RPC tests

on:
  workflow_dispatch:

jobs:
  rpc-test:
    name: ${{ matrix.settings }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        settings:
          - settings_shadownet.json
          - settings_ushuaianet.json
    env:
      NETEZOS_TEST_SETTINGS: ${{ matrix.settings }}
    steps:
      - uses: actions/checkout@v7

      - name: Build
        run: dotnet build Netezos.Tests --configuration Release --nologo

      - name: Test
        run: >
          dotnet test Netezos.Tests
          --no-build
          --configuration Release
          --nologo
          --verbosity normal
          --filter "FullyQualifiedName~Rpc"
